# 메했니?

사용자가 자신의 NEXON Open API 키로 KMS 계정 캐릭터의 반복 활동 수행 여부를 확인하는 비공식 공개 웹 서비스다. 제품 설명에는 `메이플 스케줄러 자동 체크`를 사용한다.

## Language

**사용자**:
자신의 NEXON Open API 키를 브라우저에 입력해 본인 KMS 계정 데이터를 조회하는 사람.
_Avoid_: 회원, 게스트, 넥슨 로그인 사용자

**사용자 API 키**:
사용자가 NEXON Open API에서 발급받아 본인 계정 데이터를 조회하는 비밀 인증 값. 검증 성공 즉시 현재 브라우저의 `localStorage`에 평문으로 저장하지만 서비스 서버, 로그 또는 제3자에게 전달하지 않는다.
_Avoid_: 서비스 API 키, 로그인 토큰, 공유 키

**저장된 API 키**:
검증 성공 후 현재 브라우저의 `localStorage`에 평문으로 유지되는 사용자 API 키. 브라우저 데이터를 삭제하거나 사용자가 키 삭제를 실행할 때 제거된다.
_Avoid_: 암호화된 키, 서버 저장 키, 동기화된 키

**API 키 교체**:
새 사용자 API 키를 검증한 뒤 기존 계정 캐릭터, 수행 상태 및 캐릭터별 설정을 삭제하고 새 계정 데이터를 불러오는 동작.
_Avoid_: 키 갱신, 계정 병합

**API 키 확인 필요**:
사용자 API 키가 유효하지 않거나 사용할 수 없어 자동 추적을 갱신하지 못하는 상태. 기존 로컬 데이터는 사용자가 삭제하기 전까지 마지막으로 확인된 상태로 유지한다.
_Avoid_: 동기화 지연, 데이터 없음

**계정 캐릭터**:
사용자 API 키로 조회된 KMS 계정의 보유 캐릭터. 닉네임을 직접 입력해 추가한 캐릭터는 포함하지 않는다.
_Avoid_: 등록 캐릭터, 수동 캐릭터

**활성 추적 캐릭터**:
계정 캐릭터 중 사용자가 선택해 대시보드와 수행 상태 동기화에 포함한 캐릭터. 한 사용자는 최대 10개까지 선택할 수 있다.
_Avoid_: 계정 캐릭터, 등록 캐릭터, 전체 캐릭터

**비활성 계정 캐릭터**:
계정 캐릭터 목록과 캐릭터별 설정에는 유지되지만 대시보드 및 수행 상태 동기화에서 제외된 캐릭터. 활성 추적을 해제해도 캐릭터별 설정은 보존된다.
_Avoid_: 삭제된 캐릭터, 최근 해제된 캐릭터

**스케줄러 항목**:
KMS의 메이플 스케줄러에 등록할 수 있는 콘텐츠, 퀘스트 또는 보스. 일반적인 의미의 모든 퀘스트를 뜻하지 않는다.
_Avoid_: 퀘스트, 할 일

**추적 항목**:
스케줄러 항목 중 게임 내 메이플 스케줄러에 등록된 항목. 미등록 항목은 대시보드에 표시하거나 수행률에 포함하지 않는다.
_Avoid_: 미등록 항목, 전체 스케줄러 항목

**숨긴 항목**:
사용자가 특정 활성 추적 캐릭터에서 표시하지 않기로 한 추적 항목. 수행률과 미완료 수에서 제외되며 다시 복원할 수 있다.
_Avoid_: 완료 항목, 삭제된 항목

**수행 상태**:
활성 추적 캐릭터가 특정 스케줄러 항목을 해당 일간 또는 주간 주기 안에 달성했는지를 나타내는 API 기반 상태.
_Avoid_: 수동 체크, 완료 기록

**진행 횟수**:
추적 항목에서 API가 제공하는 현재 수행 횟수와 목표 횟수. 상세 화면에서 `현재/목표` 형식으로 표시한다.
_Avoid_: 완료율, 수행 상태

**현재 주기**:
각 스케줄러 항목의 일간 또는 주간 초기화 이후부터 다음 초기화 전까지의 기간. 서비스는 지난 주기의 수행 이력을 보관하지 않는다.
_Avoid_: 수행 이력, 통계 기간

**자동 추적**:
활성 추적 캐릭터의 수행 상태를 사용자 브라우저가 스케줄러 API에서 직접 가져오는 유일한 추적 방식.
_Avoid_: 실시간 추적, 자동 체크, 수동 추적

**캐릭터 가져오기**:
사용자 API 키로 계정 캐릭터 목록을 불러와 사용자가 활성 추적 캐릭터를 선택하는 절차. 목록을 불러오는 것만으로 모든 캐릭터가 자동 추적되지는 않는다.
_Avoid_: 수동 등록, 닉네임 등록, 전체 자동 등록

**계정 캐릭터 동기화**:
사용자 API 키로 계정 캐릭터 목록의 추가 및 변경 사항을 자동으로 갱신하는 절차. 새로 발견된 캐릭터를 활성 추적 캐릭터로 자동 선택하지는 않는다.
_Avoid_: 수동 등록, 전체 자동 등록, 수행 상태 동기화

**확인 중 캐릭터**:
정상적으로 완료된 최신 계정 캐릭터 동기화에서 처음 누락되어 재확인을 기다리는 활성 추적 캐릭터. API 호출 실패는 누락으로 간주하지 않는다.
_Avoid_: 삭제된 캐릭터, 동기화 실패 캐릭터

**최근 해제된 캐릭터**:
24시간 이후의 정상적인 재동기화에서도 누락되어 활성 추적에서 자동 해제됐지만 설정 복구를 위해 7일 동안 보관되는 캐릭터.
_Avoid_: 활성 추적 캐릭터, 삭제된 캐릭터

## 수행 상태

**완료**:
스케줄러 API가 현재 주기의 달성을 직접 확인한 상태. 일반 콘텐츠는 목표 횟수가 0보다 크고 현재 횟수가 목표 횟수 이상일 때 완료로 해석한다.

**미완료**:
스케줄러 API가 현재 주기의 미달성을 직접 확인한 상태. 일반 콘텐츠는 목표 횟수가 0보다 크고 현재 횟수가 목표 횟수보다 작을 때 미완료로 해석한다.

**확인 불가**:
유효한 스케줄러 데이터가 없어 현재 주기의 달성 여부를 판단할 수 없는 상태. 미완료를 의미하지 않는다.
_Avoid_: 미완료, 동기화 지연

**마지막 동기화 시각**:
자동 추적 상태를 스케줄러 API에서 가장 최근에 가져온 시각. 게임 데이터가 실제로 갱신된 시각과 같다고 보장하지 않는다.
_Avoid_: 완료 시각, 실시간 시각

**동기화 지연**:
API 조회 실패로 최신 수행 상태를 가져오지 못해 이전에 성공한 상태를 계속 보여 주는 상황. 미완료 상태나 데이터 없음으로 취급하지 않는다.
_Avoid_: 미완료, 초기화

**새 주기 확인 중**:
한국 시간 기준 일간 또는 주간 초기화 경계를 지났지만 새 주기의 스케줄러 조회가 아직 성공하지 않은 상태. 이전 주기 캐시는 완료 집계에서 제외하며 새 항목을 미완료로 추측하지 않는다.
_Avoid_: 미완료, 동기화 지연, 확인 불가

**부분 동기화**:
여러 활성 추적 캐릭터 중 일부의 조회만 성공한 결과. 성공한 캐릭터는 갱신하고 실패한 캐릭터는 마지막 성공 상태와 동기화 지연을 유지한다.
_Avoid_: 전체 동기화 실패, 전체 동기화 완료

**로컬 데이터 복구**:
IndexedDB 마이그레이션 또는 데이터 읽기가 복구 불가능하게 실패했을 때 사용자 확인 후 캐릭터 선택, 항목 설정 및 상태 스냅샷을 초기화하고 다시 구성하는 절차. 저장된 API 키와 소규모 환경 설정은 유지한다.
_Avoid_: 자동 초기화, API 키 삭제, 계정 초기화

**모든 데이터 삭제**:
사용자 확인 후 저장된 API 키, 캐릭터 선택, 항목 설정, 수행 상태 및 앱 캐시를 현재 브라우저에서 제거하고 첫 설정 화면으로 돌아가는 동작.
_Avoid_: API 키 교체, 로컬 데이터 복구, 로그아웃

**오프라인 상태**:
네트워크 연결이 없어 마지막으로 성공한 수행 상태를 확인 시각과 함께 읽기 전용으로 보여 주는 상태. 최신 수행 상태를 의미하지 않는다.
_Avoid_: 동기화 완료, 현재 상태

**오래된 데이터**:
마지막 성공 동기화 후 30일이 지나 대시보드에 표시할 수 없는 API 기반 데이터. 다시 표시하려면 넥슨 API 갱신이 먼저 성공해야 하며 사용자 설정은 이 상태에서도 유지한다.
_Avoid_: 동기화 지연, 오프라인 상태, 마지막 상태

**새 버전 준비됨**:
서비스 워커가 새로운 앱 파일을 내려받았지만 사용자가 적용하지 않아 현재 화면에는 아직 활성화하지 않은 상태. 화면을 강제로 새로고침하지 않는다.
_Avoid_: 업데이트 완료, 동기화 필요

**비공개 대시보드**:
현재 브라우저에 구성된 활성 추적 캐릭터와 수행 상태를 보여 주며 공개 탐색이나 제3자 열람을 허용하지 않는 화면.
_Avoid_: 프로필, 공개 캐릭터 페이지

**격리 광고**:
대시보드와 다른 출처의 제한된 프레임에서 실행되어 사용자 API 키와 앱 저장소에 접근할 수 없는 광고. 앱 문서에 직접 설치되는 광고 스크립트는 허용하지 않는다.
_Avoid_: 인라인 광고 스크립트, 추적 스크립트

**광고 슬롯**:
향후 격리 광고 또는 정적 후원 배너를 표시할 수 있도록 마련한 대시보드 위치. MVP에서는 비활성화하며 빈 공간을 남기지 않는다.
_Avoid_: 활성 광고, 광고 자리표시자

**오류 코드**:
사용자가 문제 유형을 구분하고 GitHub Issues에서 문의할 수 있도록 오류 안내에 함께 표시하는 짧고 비밀 정보가 없는 식별자. API 키, 캐릭터 정보 또는 API 응답은 포함하지 않는다.
_Avoid_: 진단 정보, 오류 로그, 자동 오류 보고
